import 'package:flutter/material.dart';

class MyCenterTextWidget extends StatelessWidget {
  const MyCenterTextWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Hello World_2A Shinta Yulistiana'),
    );
  }
}
